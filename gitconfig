[core]
	editor = vim

[user]
	email = smccollum@mccollum.enterprises
	name = Sam McCollum
[credential]
	helper = cache --timeout=7200
[alias]
	lff = log --follow --graph --decorate --
	lg = log --graph --decorate
	lgl = log --graph --decorate --oneline
	st = status
	ap = add -p
	cb = checkout -b
	ci = commit
	cs = commit -s
	cm = commit -m
	csm = commit -S -m
	co = checkout
	coa = checkout -- .
	cop = checkout -p
	com = checkout master
	cfd = clean -fd
	sh = show HEAD
	cp = cherry-pick
	br = branch
	brd = branch -D
	ca = commit --amend
	csa = commit -S --amend
	can = commit --amend --no-edit
	csan = commit -S --amend --no-edit
	ds = diff --staged
	db = diff master...HEAD
	ph = push
	pf = push --force
	psu = push -u origin HEAD
	pl = pull
	plf = reset --hard
	rao = remote add origin
	rau = remote add upstream
	rs = remote show
	rso = remote show origin
	rsu = remote show upstream
	rpo = remote prune origin
	rpu = remote prune upstream
	rbi = rebase -i
	rbim = rebase -i master
